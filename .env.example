# Optional: leave unset to use Jupyter's default random token auth
# JUPYTER_TOKEN=change-me

# Optional: use a hashed password from:
# python -c "from jupyter_server.auth import passwd; print(passwd())"
# JUPYTER_PASSWORD=argon2:...
